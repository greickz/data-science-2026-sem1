import pandas

df_semi = pandas.read_json('dadosSemi.json')

print(df_semi) # o head ou outra parte da tabela como tail é para limitar o que vai ser mostrado, como por exemplo 2, vai ser somente os 2 primeiros()